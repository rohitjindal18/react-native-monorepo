overwrite: true
schema: "server/schems.js"

generates:
  src/generated/graphql.tsx:
    config:
          noNamespaces: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"